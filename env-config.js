// Configuração de ambiente - deixar vazio para usar fallbacks no app.js
// Em produção (Render), as variáveis serão injetadas via servidor

// Este arquivo existe apenas para não quebrar o import
// As variáveis de ambiente serão configuradas no Render.com